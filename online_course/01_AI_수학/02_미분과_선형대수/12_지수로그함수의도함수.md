# 지수·로그함수의 도함수
## 학습 내용
- 다항함수 도함수 유도에 이어 지수함수와 로그함수의 도함수를 유도함
- 자연상수 `e`: 약 2.718
- 괄호 안이 `1+x`인지 `1+1/x`인지에 따라 극한에서 `x`가 어디로 가는지가 달라짐
- 괄호 안의 값이 2.718로 수렴하려면 1 근처에서 변필요
- `1+x`가 1 근처가 되려면 `x`가 0으로 가고, `1+1/x`가 1 근처가 되려면 `x`가 커져야 함
