# 파이썬 4강
## 학습 포인트
Python 조건문 `if`를 학습. `a=10`을 두고 `if a == 10:` 형태로 조건을 검사하는 예시를 시작. Python은 들여쓰기가 문법적으로 중요하며, Tab이나 자동 들여쓰기를 사용. 들여쓰기가 맞지 않으면 코드가 실행되지 않을 수 있음.
## 학습 메모
조건문은 사용 빈도가 높아 직접 작성하는 연습이 중요. 콜론 오타와 들여쓰기 오류에 주의 필요.
