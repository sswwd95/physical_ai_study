# 실습 397 — portfolio_case_study

## 1. 학습 목표
포트폴리오용 문제·접근·결과 문서를 생성합니다.

## 2. Antigravity용 예제 소스 생성 하네스 프롬프트
```text
문제·접근·결과·기여가 포함된 포트폴리오 사례 문서를 생성하라.
```

## 3. 실행 명령
```bat
conda activate semi-physical-ai-stage20
python examples\ex397_portfolio_case_study.py
```

## 4. 예상 결과
요청한 종합 프로젝트·포트폴리오 산출물이 생성됩니다.

## 5. 라인별 해설

| 줄 | 코드 | 쉬운 해설 |
|---:|---|---|
| 1 | `from pathlib import Path` | 필요한 라이브러리나 모델을 불러옵니다. |
| 2 | `import json` | 필요한 라이브러리나 모델을 불러옵니다. |
| 3 | `import numpy as np` | 필요한 라이브러리나 모델을 불러옵니다. |
| 4 | `import pandas as pd` | 필요한 라이브러리나 모델을 불러옵니다. |
| 5 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 6 | `ROOT = Path(__file__).resolve().parents[1]` | 계산 결과나 설정값을 변수에 저장합니다. |
| 7 | `DATA_FILE = ROOT / "data" / "final_project_data.csv"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 8 | `CONFIG_FILE = ROOT / "config" / "project_config.json"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 9 | `OUTPUT_DIR = ROOT / "outputs"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 10 | `MODEL_DIR = ROOT / "models"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 11 | `REPORT_DIR = ROOT / "reports"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 12 | `PORTFOLIO_DIR = ROOT / "portfolio"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 13 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 14 | `for directory in [OUTPUT_DIR, MODEL_DIR, REPORT_DIR, PORTFOLIO_DIR]:` | 여러 모델·지표·장비를 반복 처리합니다. |
| 15 | `    directory.mkdir(exist_ok=True)` | 계산 결과나 설정값을 변수에 저장합니다. |
| 16 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 17 | `scorecard_file=REPORT_DIR/"kpi_scorecard.csv"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 18 | `scorecard=pd.read_csv(scorecard_file) if scorecard_file.exists() else pd.DataFrame()` | 종합 프로젝트 데이터를 읽습니다. |
| 19 | `text=f"""# 반도체 Physical AI 종합 프로젝트 사례` | 계산 결과나 설정값을 변수에 저장합니다. |
| 20 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 21 | `## 문제` | 실행되지 않는 설명 주석입니다. |
| 22 | `공정 센서와 장비 상태 데이터를 이용해 수율 저하와 고장 위험을 조기에 파악한다.` | 종합 프로젝트 통합 단계를 수행합니다. |
| 23 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 24 | `## 접근` | 실행되지 않는 설명 주석입니다. |
| 25 | `1. 데이터 계약과 품질 게이트` | 종합 프로젝트 통합 단계를 수행합니다. |
| 26 | `2. 공통 특징공학` | 종합 프로젝트 통합 단계를 수행합니다. |
| 27 | `3. 수율·고장·RUL 세 모델` | 종합 프로젝트 통합 단계를 수행합니다. |
| 28 | `4. 통합 의사결정 엔진` | 종합 프로젝트 통합 단계를 수행합니다. |
| 29 | `5. 안전 게이트` | 종합 프로젝트 통합 단계를 수행합니다. |
| 30 | `6. 운영 KPI와 보고서` | 종합 프로젝트 통합 단계를 수행합니다. |
| 31 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 32 | `## 결과` | 실행되지 않는 설명 주석입니다. |
| 33 | `{scorecard.to_markdown(index=False) if not scorecard.empty else "실습 395 실행 후 KPI가 채워집니다."}` | 계산 결과나 설정값을 변수에 저장합니다. |
| 34 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 35 | `## 기여` | 실행되지 않는 설명 주석입니다. |
| 36 | `- 종합 파이프라인 설계` | 종합 프로젝트 통합 단계를 수행합니다. |
| 37 | `- 모델 평가 자동화` | 종합 프로젝트 통합 단계를 수행합니다. |
| 38 | `- 운영 의사결정·안전 우선 로직` | 종합 프로젝트 통합 단계를 수행합니다. |
| 39 | `- 재현 가능한 산출물 구조` | 종합 프로젝트 통합 단계를 수행합니다. |
| 40 | `"""` | 종합 프로젝트 통합 단계를 수행합니다. |
| 41 | `file=PORTFOLIO_DIR/"portfolio_case_study.md"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 42 | `file.write_text(text,encoding="utf-8")` | 계산 결과나 설정값을 변수에 저장합니다. |
| 43 | `print(file)` | 결과를 콘솔에 출력합니다. |

## 6. 실무 확인 질문
1. 요구사항과 구현·평가 결과가 추적 가능한가?
2. 데이터 누수·안전·운영 실패 시나리오를 검토했는가?
3. 포트폴리오에서 문제·해결·성과를 수치로 설명할 수 있는가?