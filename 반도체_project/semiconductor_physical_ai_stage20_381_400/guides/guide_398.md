# 실습 398 — presentation_summary

## 1. 학습 목표
최종 발표용 1페이지 요약을 생성합니다.

## 2. Antigravity용 예제 소스 생성 하네스 프롬프트
```text
최종 발표용 1페이지 요약 문서를 생성하라.
```

## 3. 실행 명령
```bat
conda activate semi-physical-ai-stage20
python examples\ex398_presentation_summary.py
```

## 4. 예상 결과
요청한 종합 프로젝트·포트폴리오 산출물이 생성됩니다.

## 5. 라인별 해설

| 줄 | 코드 | 쉬운 해설 |
|---:|---|---|
| 1 | `from pathlib import Path` | 필요한 라이브러리나 모델을 불러옵니다. |
| 2 | `import json` | 필요한 라이브러리나 모델을 불러옵니다. |
| 3 | `import numpy as np` | 필요한 라이브러리나 모델을 불러옵니다. |
| 4 | `import pandas as pd` | 필요한 라이브러리나 모델을 불러옵니다. |
| 5 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 6 | `ROOT = Path(__file__).resolve().parents[1]` | 계산 결과나 설정값을 변수에 저장합니다. |
| 7 | `DATA_FILE = ROOT / "data" / "final_project_data.csv"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 8 | `CONFIG_FILE = ROOT / "config" / "project_config.json"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 9 | `OUTPUT_DIR = ROOT / "outputs"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 10 | `MODEL_DIR = ROOT / "models"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 11 | `REPORT_DIR = ROOT / "reports"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 12 | `PORTFOLIO_DIR = ROOT / "portfolio"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 13 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 14 | `for directory in [OUTPUT_DIR, MODEL_DIR, REPORT_DIR, PORTFOLIO_DIR]:` | 여러 모델·지표·장비를 반복 처리합니다. |
| 15 | `    directory.mkdir(exist_ok=True)` | 계산 결과나 설정값을 변수에 저장합니다. |
| 16 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 17 | `summary="""# 최종 발표 1페이지 요약` | 계산 결과나 설정값을 변수에 저장합니다. |
| 18 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 19 | `## 프로젝트` | 실행되지 않는 설명 주석입니다. |
| 20 | `반도체 Physical AI 기반 수율·고장·RUL 통합 예측` | 종합 프로젝트 통합 단계를 수행합니다. |
| 21 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 22 | `## 핵심 문제` | 실행되지 않는 설명 주석입니다. |
| 23 | `센서 이상과 공정 편차를 조기에 감지해 품질 손실과 정지 위험을 줄인다.` | 종합 프로젝트 통합 단계를 수행합니다. |
| 24 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 25 | `## 해결 구조` | 실행되지 않는 설명 주석입니다. |
| 26 | `데이터 품질 → 특징공학 → 3개 모델 → 통합 의사결정 → 안전 게이트 → 운영 보고` | 종합 프로젝트 통합 단계를 수행합니다. |
| 27 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 28 | `## 주요 산출물` | 실행되지 않는 설명 주석입니다. |
| 29 | `- 수율 예측` | 종합 프로젝트 통합 단계를 수행합니다. |
| 30 | `- 고장 확률` | 종합 프로젝트 통합 단계를 수행합니다. |
| 31 | `- RUL 예측` | 종합 프로젝트 통합 단계를 수행합니다. |
| 32 | `- 장비별 행동 추천` | 종합 프로젝트 통합 단계를 수행합니다. |
| 33 | `- 안전 우선 정책` | 종합 프로젝트 통합 단계를 수행합니다. |
| 34 | `- KPI·오차·운영 보고서` | 종합 프로젝트 통합 단계를 수행합니다. |
| 35 | `` | 코드 구역을 구분하는 빈 줄입니다. |
| 36 | `## 시연 순서` | 실행되지 않는 설명 주석입니다. |
| 37 | `1. 데이터 검증` | 종합 프로젝트 통합 단계를 수행합니다. |
| 38 | `2. 모델 학습` | 종합 프로젝트 통합 단계를 수행합니다. |
| 39 | `3. 통합 추론` | 종합 프로젝트 통합 단계를 수행합니다. |
| 40 | `4. 의사결정` | 종합 프로젝트 통합 단계를 수행합니다. |
| 41 | `5. 보고서 확인` | 종합 프로젝트 통합 단계를 수행합니다. |
| 42 | `"""` | 종합 프로젝트 통합 단계를 수행합니다. |
| 43 | `file=PORTFOLIO_DIR/"presentation_one_page.md"` | 계산 결과나 설정값을 변수에 저장합니다. |
| 44 | `file.write_text(summary,encoding="utf-8")` | 계산 결과나 설정값을 변수에 저장합니다. |
| 45 | `print(file)` | 결과를 콘솔에 출력합니다. |

## 6. 실무 확인 질문
1. 요구사항과 구현·평가 결과가 추적 가능한가?
2. 데이터 누수·안전·운영 실패 시나리오를 검토했는가?
3. 포트폴리오에서 문제·해결·성과를 수치로 설명할 수 있는가?