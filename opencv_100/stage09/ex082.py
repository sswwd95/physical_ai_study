# ============================================================================
# 노션 원문 학습 설명: 예제 82. ROS2 Image 메시지 이해
# ============================================================================
#
# [핵심 주제]
# ROS2에서 카메라 이미지는 `sensor_msgs/msg/Image` 메시지로 전달됨
#
# 이 메시지에는 실제 이미지 데이터뿐 아니라 이미지 크기, 인코딩, 시간 정보 등이 포함됨
#
# [실습 목표]
# 1. sensor_msgs/Image 구조 이해
# 2. height, width, encoding 의미 이해
# 3. header timestamp 이해
# 4. OpenCV 이미지와 차이 이해
#
# [실무에서 자주 하는 실수]
# 실수 1. width와 height 순서 혼동
#
# ROS2 메시지는 `height`, `width` 필드가 따로 있음
#
# OpenCV shape는 다음 순서임
#
# height, width, channels = frame.shape
#
# 하지만 좌표를 다룰 때는 보통 다음 순서임
#
# x, y
#
# 초보자가 가장 많이 헷갈리는 부분임
#
# 실수 2. encoding을 무시하고 변환
#
# 카메라가 `rgb8`로 보내는데 `bgr8`로 해석하면 색상이 바뀔 수 있음
#
# [ROS2와 연결되는 포인트]
# 카메라 노드 디버깅 시 가장 먼저 확인할 것은 다음임
#
# Topic 이름이 맞는가?
# Image 메시지가 들어오는가?
# width, height가 예상과 맞는가?
# encoding이 bgr8인지 rgb8인지 mono8인지?
# ============================================================================
# 프로그램 실행 순서를 담당하는 main 함수를 정의함
def main() -> None:
    # 현재 상태 또는 계산 결과의 터미널 출력
    print("예제 82: ROS2 Image 메시지 이해")
    print("이 예제는 설명 중심입니다. notes/ex082.md를 확인하세요.")

# 이 파일을 직접 실행했을 때만 main 함수를 호출하도록 확인
if __name__ == "__main__":
    main()
