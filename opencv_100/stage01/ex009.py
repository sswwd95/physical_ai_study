# ============================================================================
# 노션 원문 학습 설명: 예제 9. 이미지 픽셀 값 수정
# ============================================================================
#
# [핵심 주제]
# 이미지의 특정 위치 픽셀 값을 바꾸어 색을 변경함
#
# [실습 목표]
# 1. 픽셀 값 수정 방법 이해
# 2. 특정 좌표 색상 변경
# 3. 작은 영역 색상 변경
# 4. 이미지 배열 슬라이싱 이해
#
# [실무에서 자주 하는 실수]
# 실수 1. 원본 이미지가 직접 수정된다는 점을 모름
#
# 다음 코드는 `image` 자체를 수정
#
# image[120:170, 200:250] = (255, 0, 0)
#
# 원본을 보존하고 싶다면 복사본을 만들어야 함
#
# copy_image = image.copy()
# copy_image[120:170, 200:250] = (255, 0, 0)
#
# 실수 2. 슬라이싱 범위를 x, y 순서로 착각함
#
# 다음 코드는 `y범위, x범위`임
#
# image[120:170, 200:250]
#
# 즉,
#
# 세로 범위 먼저
# 가로 범위 나중
#
# [ROS2와 연결되는 포인트]
# ROS2 카메라 영상에서 객체 위치를 표시할 때 픽셀을 수정하거나 도형을 그리기
#
# 예를 들어 다음 작업과 연결됨
#
# 1. 객체 중심점에 점 찍기
# 2. 장애물 영역 표시
# 3. 라인 트레이싱 기준선 표시
# 4. 로봇 팔 Pick 위치 표시
#
# 실무에서는 픽셀을 직접 수정하기보다는 `cv2.circle()`, `cv2.rectangle()` 같은 함수를 더 많이 사용
#
# 하지만 내부 원리는 결국 픽셀 값을 바꾸는 것임
# ============================================================================
# OpenCV 기능 사용을 위한 cv2 모듈 불러오기
import cv2

# 입력 또는 출력 파일 경로 지정
image_path = "practice_images/sample.jpg"

# 지정 경로의 이미지 읽기 및 NumPy 배열 저장
image = cv2.imread(image_path)

# 이미지 또는 검출 결과 생성 여부 확인
if image is None:
    # 현재 상태 또는 계산 결과의 터미널 출력
    print("이미지를 읽을 수 없습니다.")
# 앞 조건이 거짓일 때의 실행 구간
else:
    image[100, 150] = (0, 0, 255)

    image[120:170, 200:250] = (255, 0, 0)

    # 처리 결과 확인용 OpenCV 창 표시
    cv2.imshow("Modified Image", image)
    # 키 입력 대기 및 실시간 영상 갱신
    cv2.waitKey(0)
    # 모든 OpenCV 이미지 창 닫기
    cv2.destroyAllWindows()
